[deploy]
startCommand = "php artisan config:clear && php artisan route:clear && php artisan view:clear && php artisan migrate --force && php-fpm & nginx -g 'daemon off;'"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
